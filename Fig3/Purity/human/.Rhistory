)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Number of unique peptides
num_colors <- length(unique(data$Best.Peptide))
# Generate safe color palette using RColorBrewer
max_base_colors <- brewer.pal.info["Set2", "maxcolors"]
base_colors <- brewer.pal(min(num_colors, max_base_colors), "Set2")
color_palette <- colorRampPalette(base_colors)(num_colors)
# Define patterns for each peptide type
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",        # No pattern
data$Peptide_Type == "Di" ~ "stripe",        # Stripes
data$Peptide_Type == "Tri" ~ "crosshatch",   # Crosshatch
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output JPEG
#jpeg(filename = "top20_multiinter.jpg", width = 1500, height = 1200, res = 250, quality = 50)
# Plot
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = color_palette) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)  # For adding patterns
library(dplyr)
library(RColorBrewer)  # For color palettes
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",      # Single-letter = Mono
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",     # Two-letter = Di
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",    # Three-letter = Tri
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Number of unique peptides
num_colors <- length(unique(data$Best.Peptide))
# Generate safe color palette using RColorBrewer
max_base_colors <- brewer.pal.info["Set2", "maxcolors"]
base_colors <- brewer.pal(min(num_colors, max_base_colors), "Set2")
color_palette <- colorRampPalette(base_colors)(num_colors)
# Define patterns for each peptide type
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",        # No pattern
data$Peptide_Type == "Di" ~ "stripe",        # Stripes
data$Peptide_Type == "Tri" ~ "crosshatch",   # Crosshatch
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output JPEG
jpeg(filename = "top20_multiinter.jpg", width = 1500, height = 1200, res = 250, quality = 50)
# Plot
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = color_palette) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)  # For adding patterns
library(dplyr)
library(RColorBrewer)  # For color palettes
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",      # Single-letter = Mono
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",     # Two-letter = Di
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",    # Three-letter = Tri
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Number of unique peptides
num_colors <- length(unique(data$Best.Peptide))
# Generate safe color palette using RColorBrewer
max_base_colors <- brewer.pal.info["Set2", "maxcolors"]
base_colors <- brewer.pal(min(num_colors, max_base_colors), "Set2")
color_palette <- colorRampPalette(base_colors)(num_colors)
# Define patterns for each peptide type
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",        # No pattern
data$Peptide_Type == "Di" ~ "stripe",        # Stripes
data$Peptide_Type == "Tri" ~ "crosshatch",   # Crosshatch
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output JPEG
jpeg(filename = "top20_multiinter.jpg", width = 2000, height = 1500, res = 250, quality = 50)
# Plot
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = color_palette) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)  # For adding patterns
library(dplyr)
library(RColorBrewer)  # For color palettes
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",      # Single-letter = Mono
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",     # Two-letter = Di
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",    # Three-letter = Tri
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Number of unique peptides
num_colors <- length(unique(data$Best.Peptide))
# Generate safe color palette using RColorBrewer
max_base_colors <- brewer.pal.info["Set2", "maxcolors"]
base_colors <- brewer.pal(min(num_colors, max_base_colors), "Set2")
color_palette <- colorRampPalette(base_colors)(num_colors)
# Define patterns for each peptide type
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",        # No pattern
data$Peptide_Type == "Di" ~ "stripe",        # Stripes
data$Peptide_Type == "Tri" ~ "crosshatch",   # Crosshatch
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output JPEG
jpeg(filename = "top20_multiinter.jpg", width = 2500, height = 2000, res = 250, quality = 50)
# Plot
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = color_palette) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Get distinct colors using Polychrome
num_colors <- length(unique(data$Best.Peptide))
distinct_colors <- createPalette(num_colors, seedcolors = c("#000000", "#FFFFFF"))
# Map colors to peptides in correct order
names(distinct_colors) <- levels(data$Best.Peptide)
# Define patterns
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",
data$Peptide_Type == "Di" ~ "stripe",
data$Peptide_Type == "Tri" ~ "crosshatch",
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Save JPEG
jpeg(filename = "top20_multiinter.jpg", width = 1500, height = 1200, res = 250, quality = 50)
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = distinct_colors) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Get distinct colors using Polychrome
num_colors <- length(unique(data$Best.Peptide))
distinct_colors <- createPalette(num_colors, seedcolors = c("#000000", "#FFFFFF"))
# Map colors to peptides in correct order
names(distinct_colors) <- levels(data$Best.Peptide)
# Define patterns
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",
data$Peptide_Type == "Di" ~ "stripe",
data$Peptide_Type == "Tri" ~ "crosshatch",
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Save JPEG
jpeg(filename = "top20_multiinter.jpg", width = 2500, height = 2000, res = 250, quality = 50)
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = distinct_colors) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Get distinct colors using Polychrome
num_colors <- length(unique(data$Best.Peptide))
distinct_colors <- createPalette(num_colors, seedcolors = c("#000000", "#FFFFFF"))
# Map colors to peptides in correct order
names(distinct_colors) <- levels(data$Best.Peptide)
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types (Mono, Di, Tri)
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is at the end
data$Best.Peptide <- factor(data$Best.Peptide, levels = c(setdiff(unique(data$Best.Peptide), "Remaining"), "Remaining"))
# Get distinct colors using Polychrome
num_colors <- length(unique(data$Best.Peptide))
distinct_colors <- createPalette(num_colors, seedcolors = c("#000000", "#FFFFFF"))
# Map colors to peptides in correct order
names(distinct_colors) <- levels(data$Best.Peptide)
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is last in order
unique_peptides <- unique(data$Best.Peptide)
ordered_peptides <- c(setdiff(unique_peptides, "Remaining"), "Remaining")
data$Best.Peptide <- factor(data$Best.Peptide, levels = ordered_peptides)
# Generate colors
num_peptides <- length(levels(data$Best.Peptide))
distinct_colors <- createPalette(num_peptides, seedcolors = c("#000000", "#FFFFFF"))
names(distinct_colors) <- levels(data$Best.Peptide)  # Safe mapping
# Define patterns
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",
data$Peptide_Type == "Di" ~ "stripe",
data$Peptide_Type == "Tri" ~ "crosshatch",
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output plot
jpeg(filename = "top20_multiinter.jpg", width = 1500, height = 1200, res = 250, quality = 50)
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = distinct_colors) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/multiinter")
library(ggplot2)
library(ggpattern)
library(dplyr)
library(Polychrome)
# Read data
data <- read.delim("top_20.csv", header = TRUE, sep = "\t")
# Convert columns to factors
data$No..of.Tools <- factor(data$No..of.Tools)
# Assign peptide types
data$Peptide_Type <- case_when(
grepl("^[A-Z]$", data$Best.Peptide) ~ "Mono",
grepl("^[A-Z]{2}$", data$Best.Peptide) ~ "Di",
grepl("^[A-Z]{3}$", data$Best.Peptide) ~ "Tri",
TRUE ~ "Other"
)
# Ensure "Remaining" is last in order
unique_peptides <- unique(data$Best.Peptide)
ordered_peptides <- c(setdiff(unique_peptides, "Remaining"), "Remaining")
data$Best.Peptide <- factor(data$Best.Peptide, levels = ordered_peptides)
# Generate colors
num_peptides <- length(levels(data$Best.Peptide))
distinct_colors <- createPalette(num_peptides, seedcolors = c("#000000", "#FFFFFF"))
names(distinct_colors) <- levels(data$Best.Peptide)  # Safe mapping
# Define patterns
data$Pattern <- factor(case_when(
data$Peptide_Type == "Mono" ~ "none",
data$Peptide_Type == "Di" ~ "stripe",
data$Peptide_Type == "Tri" ~ "crosshatch",
TRUE ~ "none"
), levels = c("none", "stripe", "crosshatch"))
# Output plot
jpeg(filename = "top20_multiinter.jpg", width = 2500, height = 2000, res = 250, quality = 50)
ggplot(data, aes(x = No..of.Tools, y = Proportion, fill = Best.Peptide, pattern = Pattern)) +
geom_bar_pattern(stat = "identity",
pattern_color = "black",
pattern_fill = "black",
pattern_density = 0.02,
pattern_spacing = 0.01,
pattern_size = 0.2) +
scale_fill_manual(values = distinct_colors) +
scale_pattern_manual(values = c("none" = "none", "stripe" = "stripe", "crosshatch" = "crosshatch"),
labels = c("none" = "Monopeptide", "stripe" = "Dipeptide", "crosshatch" = "Tripeptide")) +
labs(title = "Most common peptides detected in LCRs across intersections of various tools",
x = "Number of Tools",
y = "Proportion",
fill = "Peptide",
pattern = "Peptide Type") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
guides(
fill = guide_legend(override.aes = list(pattern = "none")),
pattern = guide_legend(override.aes = list(fill = "white"))
)
dev.off()
setwd("/home/anirjit/ANIRJIT/RESULTS/Purity/human")
library(ggplot2)
library(dplyr)
# Load the data
data <- read.table("entity_counts.tsv", header=TRUE, sep="\t", stringsAsFactors=FALSE)
# Convert Filename to factor for better grouping
data$Filename <- factor(data$Filename, levels = unique(data$Filename))
# Define custom colors for files
custom_colors <- c("#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd",
"#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf")
# Plot
ggplot(data, aes(x = Filename, y = Proportion, fill = as.factor(Purity))) +
geom_bar(stat = "identity", position = "dodge") +
labs(title = "Proportion of Entities Retained per Tool at Different Purities",
x = "Tool",
y = "Proportion of Entities Retained",
fill = "Purity Level") +
scale_fill_manual(values = custom_colors) +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) # Rotates labels for readability
setwd("/home/anirjit/ANIRJIT/RESULTS/Purity/human")
library(ggplot2)
library(dplyr)
# Load the data
data <- read.table("entity_counts.tsv", header=TRUE, sep="\t", stringsAsFactors=FALSE)
# Calculate slopes for each tool
slopes <- data %>%
group_by(Filename) %>%
summarize(Slope = coef(lm(Proportion ~ Purity, data = .))[2]) # Extracts slope from linear model
# Print slopes
print(slopes)
# Plot without grid lines
ggplot(data, aes(x = Purity, y = Proportion, color = Filename, group = Filename)) +
geom_line(size = 1) +
geom_point() +
labs(title = "Effect of Purity on Proportion of Retained Entities",
x = "Purity Level",
y = "Proportion of Entities Retained") +
theme_minimal() +
theme(panel.grid = element_blank())  # Removes background grid
